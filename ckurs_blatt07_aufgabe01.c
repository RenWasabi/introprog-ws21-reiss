#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
    char wochentag[] = "Freitag";
    int tag = 13;
    char monat[] = "Mai";
    int jahr = 1927;
    char *string; // Hier soll das Datum hineingeschrieben werden!
    const size_t kapazitaet; // Maximale Laenge des Datumstrings!

    // Hier implementieren und dynamisch Speicher reservieren

    printf("%s\n", string);
    // Speicher freigeben

    return 0;
}
